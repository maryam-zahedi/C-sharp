# C-sharp
c# code
 if (RegularExpression.checkForEmail(textBox1.Text.ToString()))
            { label1.Text = "Valid"; }
            else
            { label1.Text = "InValid"; }
           
           #####
